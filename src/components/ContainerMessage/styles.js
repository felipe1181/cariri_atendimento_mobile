import { StyleSheet} from 'react-native';
const stylesConteinerMessage = StyleSheet.create({
  container: { },
  text:{
    fontSize:18,
    color:"#fff",
    textAlign:"center",
  }
});
export default stylesConteinerMessage;